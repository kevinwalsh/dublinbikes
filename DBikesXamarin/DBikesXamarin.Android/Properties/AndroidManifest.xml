<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android" android:versionCode="1" android:versionName="1.0" package="com.companyname.dbikesxamarin">
    <uses-sdk android:minSdkVersion="21" android:targetSdkVersion="28" />
    <application android:label="DBikesXamarin.Android"
             android:networkSecurityConfig="@xml/network_security_config"    
                 ></application>
  <!--    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />  -->
          <uses-permission android:name="android.permission.VIBRATE" />
          <uses-permission android:name="android.permission.INTERNET" />
            
              <!-- NB, android emulator can do http calls LOCALLY, but note that ALL internet calls are disabled by default!
                  i.e. adding "INTERNET" permission isnt enough for emulator to reach any remote services. 
                  SOLUTION: edit emulator launch settings, to set a DNS server
                    1) Tools-> options;  Xamarin -> Android Settings -> "Additional Emulator Launch Arguments"
                    2) value =    =dns-server 8.8.8.8
                    3) restart emulator
            -->
  
</manifest>
